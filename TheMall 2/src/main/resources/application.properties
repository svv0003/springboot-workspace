spring.application.name=TheMall
# 위 내용 지워도 상관 없다.

# String SptingBoot IO에서 Generate 형태로 프로젝트를 생성할 경우,
# 반드시 파일 인터딩 먼저 설정해야 한다.

# 인텔리제이 자체에서 SpringBoot 프로젝트를 생성할 경우,
# 기존에 저장되어 있는 인코딩 형식이 그대로 유지되어 형성된다.

# Description:
# Web server failed to start. Port 8080 was already in use.
# Action:
# Identify and stop the process that's listening on port 8080 or configure this application to listen on another port.

# SpringBoot는 8080을 기본 사용하고,
# React는 3000 port를 기본 사용한다.


# 기본 포트 8080에서 8081로 변경되어 사용 가능하다.
server.port=8081





# 동일한 DB를 다수 접속한다고 하여 문제 발생하지 않는다.
# 본인 비밀번호나 DB명칭, 접속 주소 포트 문제일 것이다.
spring.datasource.url=jdbc:mysql://localhost:3306/tje
spring.datasource.username=root
spring.datasource.password=tpdnjsdldmlsql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect



# Thymeleaf 템플릿 엔진 설정 html 파일의 위치 지정
# 정적 리소스의 경우에는 Thymeleaf 엔진 사용하지 않고 지정

# classpath = src/main/resources 단축어
#              시작 위치 src/main/resources/templates/      내부에 작성된
spring.thymeleaf.prefix=classpath:/templates/
#              확장자명  .html                              파일들
spring.thymeleaf.suffix=.html
# 아래는 생략 가능
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
#spring.thymeleaf.cache=false

# 정적 리소스 경로 설정
spring.web.resources.static-locations=classpath:/static/

